# Changelog

Notable changes for the Smart City Booking Backend.

Format based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).
Releases are tagged `v4.x.x` from branch `version/4.x`.

## [Unreleased]

### Added

- `GET /api/:tenant/accesspoints/:id/bookings` names the bookings with a live access at one access point (glossary "Laufender Zugang"), so the administration sees what deleting it would tear up - the delete detaches the access point but revokes nothing at the provider. Booking id, customer and period, at most ten of them - the ones already running first - with `total` counting them all. New rights-table entry `accessPoint.bookings` with `any: manageBookings.readAny`: the answer names customers, so it is the booking reader's right, which a tenant owner holds anyway. The tenant-independent `GET /api/access/access-points/:id/bookings` could not serve this - it hangs on `accessBookings.read` with `any: instanceOwner`, so a tenant owner would only ever see their own bookings.

- A tenant is told when a grant fails after the booking is already valid (glossary "Mitteilung"): `access.provision` is a recorded operation of the effect pipeline - the booking stays paid and confirmed while nobody can open the door or the compartment - so every transition that grants (`admit`, `confirm`, `pay`, `reinstate`, and the group's `admit`, `confirm`, `pay`) declares the notice `ACCESS_PROVISION_FAILED` as its last notify step. It goes to the tenant's address, names the bookings whose grant was recorded and the reason the provider gave, and is skipped where the grant held. A group sends one notice naming the members that failed, not one per member. The notice is not gated: a tenant that wants no notice of new bookings still gets its fault reports. It reports and does not retry - the run-behind job for recorded effects is still an open question. The pipeline hands a step's condition and its `run` the outcome so far, which `recordedFailures(outcome, adapter, op)` reads.

- Authorization (glossary "Berechtigung"), step 1 of 5: the module `src/commons/services/authorization/` - the rights table (glossary "Rechtetabelle") as data in `table.js`, the pure policy `decide(principal, resource, action)` answering the reach (glossary "Reichweite") `any | own | public | null` with the fixed precedence instance owner ⊇ tenant owner ⊇ role ⊇ signed in, the principal (glossary "Prinzipal") loaded once per request from `UserManager.getUserPermissions`, and the three route markers `authorize(resource, action)`, `public(resource?, action?)` and `tokenAuthorized()` as thin express adapters over the JWT middleware. An unknown entry throws when the router loads. No router is on it yet and no production behaviour changes: the table is tested as the matrix over five kinds of principal (`tests/authorization-policy.test.js`), every route of every router under `src/platform` is pinned with the status code each of the five principals gets today (`tests/snapshots/authorization/routes.json`, over the lifecycle harness now mounting all seven routers and a fixture world behind the remaining data managers), the invariant "every route carries one marker" lists the unmarked routes until step 5, and the two router-order comments (`/resolve-scan/:scanCode` before `/:accessPointId`, `/instance/media` before `/:tenant`) are tests. The harness drives the rights for real over the instance, the memberships and one all-role instead of stubbing `PermissionService`.

### Fixed

- A door or compartment its provider cannot serve was skipped in silence: `AccessService._provisionDoors` logged the audit row `action: provision, result: failure` for a lock that does not support the access point's mode, `_provisionCompartments` did the same for a locker provider that grants no compartments, and both then `continue`d. `provisionForBooking` resolved as if all were well, so the lifecycle's effect row read `ok` - booking paid and confirmed, door shut, the failure in the audit log and nobody told. The skipped access points are collected now and the run throws `AccessProvisionError` (`provision_incomplete`) at the end, naming each and why. The skip itself is unchanged: the other doors of the booking are still granted, what was granted is still stored, and the booker still gets the PINs that worked - the throw comes after all of it, so that the effect is recorded and the tenant gets its fault notice.

- The OpenAPI of the group bookings parsed no more: the unquoted description of `POST /group-bookings/{id}/pay` carries a colon (`` `success: true` ``), which YAML reads as a nested mapping, so the whole file was dropped and the server logged a `YAMLSemanticError` at every start. The description is quoted; every file under `src/docs/` parses, and the five group-booking operations are back in the spec.

### Removed

- `src/commons/services/permission-service.js` with its 13 checks, `UserManager.hasPermission`, `RolePermission.MANAGE_TENANTS` (a role group that never existed in the role schema), the getter `Media.ownerUserId` (it existed so the permission service would find a medium's owner), `AuthenticationController.isSignedIn` with `src/middleware/jwt-auth.js`, and `conditionalAuth` of the auth middleware (not `authenticateIfNeeded`, which lives in `commons/utilities/auth-utils.js` and the catalog controller still calls) - all without a caller since the routes decide over the rights table. The JWT verification (`requireAuth`, `optionalAuth`) stays; the markers call it.

### Changed

- The SMTP transport verifies the mail server's certificate on the STARTTLS path too, and negotiates over the Node defaults: `tls: { ciphers: "SSLv3", rejectUnauthorized: false }` is gone, so both paths of `createSmtpTransport` carry no TLS options of their own. The two settings were independent of each other. `rejectUnauthorized: false` alone accepted any certificate; `ciphers: "SSLv3"` is OpenSSL's group alias for the suites first defined in SSLv3 and helped no self-signed server at all - it only struck out the modern TLS 1.2 suites, so every STARTTLS tenant negotiated `AES256-SHA` (plain RSA key exchange, no forward secrecy) where the defaults reach `ECDHE-RSA-AES128-GCM-SHA256`. Both came in as one copied snippet when STARTTLS was added for Office 365, whose certificate is publicly valid; the implicit TLS path has verified all along. There is no opt-out: a tenant on a self-signed SMTP server needs its CA in the trust store, not a switch.

- Authorization, step 5 of 5 - the router strand is complete: every one of the 253 routes under `src/platform` carries exactly one marker, and `tests/authorization-invariants.test.js` fails on one that does not (it only listed them before). The last four routers are on it - the authentication router (18 routes `public("auth","all")`, `/signout` and `/me` `authorize("user","readSelf")`), the HTML and JSON engines (`public`), and the CSV exporter (`authorize("exporter","export")`) - so `requireAuth`, `optionalAuth` and `isSignedIn` are gone from every router. `exporter.export` became a real entry of the rights table (`manageBookables.updateOwn | updateAny`, the two levels `CsvExportController._hasPermission` checked behind the `isSignedIn` route; the entry read `own: signedIn` from step 1, which would have opened every attendee list to every signed-in user), the controller has no rights branch left, and an event out of reach answers 404. The last two places in the domain that asked for themselves take a value now (spec §5): `AccessDecision.resolveAccessRole` reads the booking at its owner key, and the two cross-tenant access-booking lists take `canManageIn(tenantId)` from the controller, which loads one principal per tenant of the result. The spec's addendum §15 lists the flips of this step and, in §15.1, every status-code flip of steps 2 to 5 - 56 route/principal cells over 30 routes, plus 93 further routes that changed in the 403 form alone.
- Behaviour change, deliberate (authorize spec §15): the refusal of the CSV export is the one JSON form of `ForbiddenError` (a bare 403 before), an event the reach does not cover answers 404 rather than 403, and an instance owner without a membership in the tenant may export (403 before - `UserManager.hasPermission` was the one rights helper without an instance-owner branch, so the export was the one route the standing precedence did not reach). The HTML snippets stay what they were: `optionalAuth` runs behind the `public()` marker now, but the engine is told the reach rather than "somebody is signed in", so a signed-in reader sees the public projection as before.

- Authorization, step 4 of 5: the routers under `api/routes/*` and `api/v2/routes/*` (about 50 routes) are on the markers - access, access points, access apps, the locker facade, the tenant catalog, iCal, tenant and instance media, checkout, the coupon lookup, the booking status; the access webhook is `tokenAuthorized()`. `isSignedIn`, `optionalAuth` and `requireAuth` are gone from all of them, and with them the rights branches of the access, access-point, access-app, audit, catalog and iCal controllers and the `PermissionService` call inside `locker.routes.js`. Operating a door reads the reach: `canManage` of `AccessDecision` is `req.reach === "any"` at `booking.operate`. The media domain stops asking (spec §5): `media-reference-guard` and `media-access` take a reach as a value, the callers that save an entity decide it a second time (`scopeFor(req, "media", "read")`), and `media.controller._scope(req)` with `TENANT_SCOPE`/`INSTANCE_SCOPE` is gone - the rights table tells the two libraries apart by the resource (`media` against `instanceMedia`). The visibility `public | intern` of a medium stays in the media module, in addition to the reach. The table gained `media.metadata` (the door of the metadata routes, which serve the library and the booking documents alike) and `ical.events`; `withinReach` joins `ownCondition` in `authorization/reach.js`, `scopeFor(req, resource, action)` asks the table a second time in an adapter. `PermissionService` stays for `AccessService` and `AccessDecision`, which step 5 removes. The route snapshot records the flips (25 routes, 24 in the 403 form alone), the spec's addendum §14 lists them.
- Behaviour change, deliberate (authorize spec §3.1 and §14): a customer downloads the iCal file of their own booking (`GET /:tenant/ical/bookings/:id`: 403 before); a booking or event the reach does not cover answers 404 rather than 403; the refusals of the access points, access apps, the locker facade, the audit export and the tenant catalog are the one JSON form of `ForbiddenError` (bare 403 and a catalog message before); `manageTenants`, which never existed in the role schema, is `tenantOwner` at the access apps; the metadata of a booking document is refused before the medium is loaded where the reach covers neither the library nor the booking.

- Authorization, step 3 of 5: the instance router `api-router.js` (59 routes) and `routes/instance.routes.js` (4 routes) are on the markers, `isSignedIn`/`optionalAuth` are gone from both, and so are the rights branches of the instance, rule, user, tenant, membership, mail-template and instance-catalog controllers and of the cross-tenant access bookings. A route about one tenant names it `:tenant` (`/api/tenants/:tenant/...`, the URL unchanged) so the principal is loaded in that tenant - `GET /api/tenants/:tenant/users` is alive again; `PUT /api/tenants` names its tenant in the body over the new marker option `tenantOf(req)`. `TenantManager.getTenants(scope, { owned })` translates `own` into the user's active memberships. The rights table gained `tenant.countCheck`, `tenant.pdfPreview`, `tenantUser.owner`, `user.create` and `instanceCatalog.readPublic` (`instanceCatalog.read` is the owner's alone). The obsolete `PUT /tenants` and `PUT /users` carry `update` and decide the creation in the adapter. `PermissionService` stays for the routers of step 4. The route snapshot records the flips (39 routes, 26 in the 403 form alone), the spec's addendum §13 lists them.
- Behaviour change, deliberate (authorize spec §3.2 and §13): the user directory (`GET /users`, `/users/ids`, `/users/:id`) answers everyone but the instance owner 403 (a tenant owner read every user of the instance, a signed-in user every single user before); a user changes the own record over `PUT /user` only (`PUT /users` and `POST /users/:id/change-id` on oneself: 403); a tenant is opened over `POST /tenants` (`PUT /tenants` with an unknown id creates for the instance owner only); the instance owner lists every tenant under `?publicTenants=true`; a refusal is the one JSON form of `ForbiddenError` and comes before the body checks (`add-user`, `add-owner`, the notification recipients without `userId`: 403 instead of 400 for a principal without the right); an unknown tenant or user answers 404 instead of 500; the instance owner adds and removes tenant owners and removes an owner without holding a membership (500 before).
- Authorization, step 2 of 5: the tenant router `api-router-tenant-related.js` (102 routes) is on the markers - `authorize(resource, action)`, `public(resource?, action?)`, `tokenAuthorized()` - and `isSignedIn`/`optionalAuth` are gone from it, as from the three routes of `api-router.js` that share a handler with it (`GET /tenants/:id/users`, `GET /roles`, `GET /bookings/assigned`). No controller of the router branches over rights any more: a handler hands `scopeOf(req)` - the reach (glossary "Reichweite") and the principal's user - to its manager, and the managers translate `own` into their query condition (`ownerUserId` at bookables, events and coupons, `assignedUserId` at bookings and group bookings; `ownCondition` in `authorization/reach.js`): lists filter in the query, a single read outside the reach answers 404. The obsolete PUT store routes carry the `update` marker and decide the creation in the adapter (`decide(principal, resource, "create")`). The rights table gained the entries a route without a public projection needs (`bookable.readPublic`, `bookable.meta`, `event.meta`, `booking.list`, `booking.lookup`, `booking.reprint`, `booking.invoice`, `groupBooking.invoice`, `role.list`, `invitation.respond`); `PermissionService` stays for the routers of step 3 and 4. The route snapshot records the flips (59 routes), the spec's addendum §12 lists them.
- Behaviour change, deliberate (authorize spec §7 and §12): a refusal is the one JSON form of `ForbiddenError` (the text and JSON bodies of invitations, coupons, group bookings and the payment test are gone); `GET /group-bookings` answers a customer the own groups only; a customer reads the own group, the own refund preview, the own bookings of a bookable and reprints the own group's receipt and cancellation receipt (403 before); `GET /bookables` and `GET /coupons` answer a signed-in user without reach 403 instead of an empty list; a record outside the reach `own` or unknown answers 404 (403 or 500 before); a role with `create` but no update level can no longer create over the obsolete PUT. Defects gone with it: `GET /roles/tenant` 500 → 200 with the own roles, the challenge writes hang → 403, `POST /challenges` creates in the tenant of the route, `GET /coupons` anonymous 500 → 401, `GET /bookings/:ids/status` 500 → 200, `GET /bookables/:id/openingHours` 500 → 200, `POST /payments` without `bookingIds` → 400 instead of no answer, `GET /payments/providers/:provider/test` checks the tenant of the route. `GET /api/tenants/:id/users` reads a tenant the route does not carry and answers everyone but the instance owner 403 (a dead route before as well; step 3 decides its fate).
- Mail stack, step 4 of 4: every notice (glossary "Mitteilung") is composed by `compose` and sent by `send`; `MailController` is gone. The payment request (glossary "Zahlungsaufforderung") is a value: `paymentRequest()` of a payment provider answers `{ form: "link" | "invoice" | "pending", paymentUrl?, files? }` and mails nothing - a provider with a payment page (pmPayment, GiroCockpit, ePayBL, the default of `PaymentService`) the link to the storefront's redirection, the invoice provider the invoice it issued or the announcement of one the administration creates - and the notify step of the booking lifecycle sends the notice of that form (`PAYMENT_LINK_AFTER_APPROVAL`, `INVOICE_AFTER_APPROVAL`, `BOOKING_CONFIRMED_INVOICE_PENDING`), three rows after `payment.requestPayment` in the effect table; the single and aggregated twins of the four providers are gone. The registry `mail-types.js` carries the seven tenant and instance notices (`family: tenant | instance`, `audience: named | instanceAdmin`) with their links: `WORKFLOW_NOTIFICATION`, `INVITATION`, `VERIFICATION_REQUEST`, `PASSWORD_RESET`, `FORGOT_PASSWORD_REQUEST`, `USER_CREATED`, `CARD_LINK_REQUEST`; the reprint of an invoice, `AccessService`, the workflow action, `UserService`, `InvitationService`, `CardAuthService` and the checkout's invoice payment call `compose` + `send`, the lazy `require`s are gone. `UserManager.resetPassword` is `UserService.resetPassword`; the write-side validation of the supervisors' recipients is `MembershipService.prepareBookingNotificationRecipients`, `SupervisorNotificationService` is gone. Every notice goes out byte-identical (the snapshots prove it)
- Behaviour change, deliberate (spec section 5.2, 5.8 and 5.9): the invitation and the workflow notification are tenant notices and go over the tenant's own mail configuration where it is complete and in use (`useInstanceMail: false`; over the instance's account before), with the tenant's media embedded; the payment request of a booking or group awaiting payment is one notice of the transition, recorded in its effect row (mailed by the provider before, with one mail per booking for a list without a group)
- Mail stack, step 3 of 4: `compose(type, ctx)` of the mail module (`mail-service/index.js`, with `send`) composes a booking notice (glossary "Mitteilung") as mail values - `compose(type, { tenantId, bookingIds, groupBookingId?, attachments?, reason?, hookId?, refundPreview?, paymentUrl?, accessPoints? })`, the form of the issuance's `issue`. The registry `mail-types.js` names each type's `family`, `audience` (booker, tenant, supervisors, organizers, named), `attachICal`, `mergeMailAttach` and `gate`; the loader reads the tenant, the bookings, their bookables (by id, no catalogue scan) and the events once; `render.js` renders without a store; `recipients.js` resolves the circle (glossary "Empfängerkreis"), an empty one being no mail; `mail-attachments.js` lives in the mail module. Attachments go issued file → `mailAttach` documents (the three confirmations) → iCal (the two confirmations and "invoice to follow") → QR code. `groupBookingId` set is the aggregated notice (glossary "Sammelmitteilung"). The mail adapter of the booking lifecycle is one operation, `send(type, ctx)` = compose + send per mail, the effect row `mail.<type>`, `skipped` where nobody gets the notice; the harness records finished mails at the transport (`mail.<type> <to> [<attachments>]`). `MailSenderService`, `MailDataService`, `_attachICalFiles`, `SupervisorNotificationService.notifySupervisorsOnBookingCreated` and the `notifyOrganizers` export are gone; `MailController` stays as a facade over `compose` for the payment providers, the reprint and the access service, and still renders the seven tenant and instance notices. Every notice goes out byte-identical (the snapshots prove it)
- Behaviour change, deliberate (spec section 5.1, 5.6 and 5.7): the payment link after approval and every other booking notice go to the recipients of their type - the `address` the facade is given is ignored, a group's payment link goes to the booker of its first member (it went to the given address before); a list of bookings without a group is refused with 400 `mail_bookings_without_group` instead of one mail per booking; a `mailAttach` document that cannot be loaded is logged as a warning and left out (an error log before); a mail to a supervisor or an organizer that fails is logged, the remaining mails still go, and the transition records the failure in its effect row (swallowed before); a notice the transport skipped as a whole (mail disabled at the instance) is a skipped effect row; the facade's aggregated calls (`aggregated: true`) look the group up and answer 404 `group_booking_not_found` where the bookings belong to none. The organizers of a group with ticket members are told per member, as before
- Mail stack, step 2 of 4: `MailerService.send(mail)` is the transport (glossary "Versandweg") and takes the mail value `{ type, tenantId | null, to, bcc?, subject, html, attachments }`; `tenantId: null` is an instance mail. It chooses the account as before - the tenant's where `useInstanceMail` is false and its configuration is complete, the instance's otherwise - and answers `{ status: "sent", transport: "instance" | "tenant", messageId? }` or `{ status: "skipped", reason: "mail_disabled" | "no_recipient" }`, or throws the transport's error after three attempts. The adapters live in `mail-service/transports/` (SMTP, Microsoft Graph, in-memory for tests). `MailController`, `MailSenderService` and the rule engine build the value (`MailerService.renderHtml` renders the shell template and embeds the media images) and call `send`; every notice goes out byte-identical (the snapshots prove it)
- Behaviour change, deliberate (spec section 5.3 to 5.5): a tenant whose mail configuration is incomplete still sends over the instance, but that is logged as a warning once per tenant configuration (silent before); mail disabled at the instance and a mail without a recipient answer `skipped` (nothing before); the transporter pool hashes the Graph client secret too, so a rotated secret gets a new transporter at once.
- Booking lifecycle, step 9 of 9: the admission of a booking (glossary "Aufnahme") is the transition `admit` of the lifecycle, single and group. The checkout (`checkout/booking-checkout.js`: `createSingleBooking`, `createGroupBooking`, `updateBooking`) prepares the booking, decides the state it starts in - `BundleCheckoutService` writes `booking.status`, the flags of the admin form are read, never stored - stores it and hands it over. `admit` holds the compartments at `requested | payment_due`, grants the access at `confirmed`, issues the receipt of a booking confirmed and paid at once, fires `onCreate` after the hold and with `skipBookingStatus` like every event, sends exactly one mail to the customer - the request confirmation, the payment request (glossary "Zahlungsaufforderung") of a manual booking awaiting payment, the confirmation with the receipt, the free booking confirmation - then the tenant's, the supervisors' and the organizer's notice; a group is held or granted per member, receipted and mailed once. A hold that fails aborts the admission: the checkout deletes the booking (every member and the group of a group checkout), gives the coupon back and answers what the hold threw. `BookingService` is queries only (refund previews, status, ownership, seats, the consistency checks `transitionErrors` and `reprintErrors`); the controllers, the payment webhook, the workflow action and the rule engine call the default instances of the lifecycle, and `commitBooking`, `setBookingPayed`, `setAggregatedBookingPayed`, `rejectBooking`, `requestRejectBooking`, `cancelBooking` and the two `handleSingleBooking*` are gone. The hard delete of `DELETE /bookings/:id` and `/group-bookings/:id` is `booking-lifecycle/booking-deletion.js` (access revoked, documents removed, booking removed). The amendment carries the hooks of the stored booking, so an open cancellation request survives the admin PUT. The prototype under `.scratch/architecture/booking-lifecycle/prototype/` is deleted
- Behaviour change, deliberate (spec part 1, 5.1; part 2, section 8 and 13): a manual booking "paid but not confirmed" (`isPayed` without `isCommitted` on a priced booking) or born cancelled is refused with 400 `invalid_status` before anything is written (it was stored as confirmed before). A booking awaiting payment gets no booking confirmation at the checkout any more: the customer at the storefront is handed the payment page, the customer of a manual booking gets the payment request of the tenant's payment provider (a confirmation without receipt or payment information before). A free booking confirmed at once gets the free booking confirmation; a booking confirmed and paid at once gets the confirmation with the receipt **and** its `mailAttach` documents (the receipt replaced them before), and the checkout answers the booking with the receipt. A grant that fails at the admission of a booking paid at once is recorded and the booking stands (rolled back before); the hold before it still refuses a booking that finds no compartment. `onCreate` no longer calls back into the booking state asynchronously; a mail that fails at admission is recorded per mail, the others go out. A group whose members would start in different states is refused by the group's guard, 409 `invalid_transition`, and never exists; a hold that fails at one member deletes every member and the group (the first member stayed, without a group, before). An aborted payment at the webhook still answers 500 `set_booking_payed_failed` / `set_aggregated_booking_payed_failed`; the workflow action's `commit` runs without the payment provider consistency check (the entity requires a provider anyway)
- Booking lifecycle, step 8 of 9: a group booking runs through its own lifecycle, `booking-lifecycle/group-booking-lifecycle.js` (`admit | confirm | pay | cancel(tenantId, groupBookingId, options)`), over the same pipeline and adapters as a single booking: the state write and the access per member, one aggregated document and one mail for the group, the workflow event per member. A group has no state of its own (glossary "Gruppenzustand"); a transition demands its members in one shared state. `POST /group-bookings/:id/commit`, `/pay` and `/reject` call the lifecycle, the consistency checks of commit and reject keep their `{ success: false, errors }` answer; `BookingService.setAggregatedBookingPayed` delegates for the payment webhook, `commitGroupBooking`, `rejectGroupBooking` and `handleAggregatedBookingConfirmation` are gone. The group checkout hands the stored group to `admit`
- Behaviour change, deliberate (spec part 2, 4.2, 4.3 and 13): a state write that fails at member k of a group restores the members before it, 500 (a half-changed group before). Confirming, paying or cancelling a group that is not in the state the transition needs, at the endpoints and the aggregated webhook, is 409 `invalid_transition` (a second document or confirmation before); an unknown group is 404. The aggregated payment fires `onPay` per member and its confirmation carries the receipt and the members' `mailAttach` documents; a webhook naming only some members pays the whole group. The group's cancellation document is issued after the state writes, `alreadyPaid` reads off the state cancelled from; a receipt, document or mail that fails is recorded (200 instead of 500). The organizer of a ticket member is told on confirmation. Workflow events and grants of a group follow all its writes
- Booking lifecycle, step 7 of 9: the amendment of a booking (glossary "Änderung") is the transition `amend` of the lifecycle: the content write in the state the booking is in (whatever state or refund audit the form carries, the stored ones stand), then the access following the content - moved along at `confirmed`, the compartments held anew at `requested | payment_due` (the hold first, then whatever an unpaid booking still holds granted is taken back), nothing at `rejected | cancelled`; no document, no workflow event, no mail. The admin PUT (`PUT /bookings`) runs the plan of `update-plan.js`: `BookingService.updateBooking(tenantId, booking, { requestBody, userId })` prepares the booking, reads the three flags of the form against the state the booking is in and runs `amend` and then the transitions the flags ask for (`confirm`, `pay`, `cancel`, `reinstate`), each atomic for itself, all as the administration; the cancellation of a flipped `isRejected` refunds in full and names the user who flipped it
- Behaviour change, deliberate (spec part 1, section 6): flags no sequence of transitions reaches - `isPayed` without `isCommitted` on a priced booking, a confirmed booking sent back to a request, a cancelled or rejected booking reinstated with other flags than the state it was cancelled from (`isRejected: false` together with `isCommitted` at a rejected request, which step 6 ran as `reinstate → confirm`) - are refused with 400 `invalid_status_change` before anything is written (the PUT stored any combination before, "paid but never confirmed" reading as confirmed). The content write of the PUT is conditional on the state the plan was made for: a booking a payment webhook moved on in between answers 409 `invalid_transition` with nothing written. There is no rollback across the transitions of a plan: where transition k fails, the ones before it stand, the content is written and the answer is the error of k (before, the old booking was written back over whatever the transitions had done, mails sent included; that restore left a stale `cancellationRefund` behind, which is gone with it). The compartments of an unpaid booking are held anew before the grants are taken back (revoke, then hold before). The flip of `isRejected` cancels as the administration with a full refund (audit `origin: admin`, `cancelledByUserId` the user; `system` before) and, at a confirmed booking, moves the access along with the content before the cancellation revokes it
- Booking lifecycle, step 6 of 9: the cancellation, the cancellation request and the reinstatement of a booking are the transitions `cancel`, `requestCancel` and `reinstate` of the lifecycle. `cancel` (`requested → rejected`, `payment_due | confirmed → cancelled`) writes the state with the reason, the refund audit and the state cancelled from (`cancellationRefund.cancelledFrom`) and drops the hook of a cancellation request in one conditional write, then revokes the access, issues the cancellation document **after** the write, fires `onReject` and mails the rejection or the cancellation with the document. `requestCancel` writes the `REJECT` hook and mails the verification with the refund preview. `reinstate` (`rejected → requested`, `cancelled →` the state cancelled from) writes the state without the refund audit and grants the access again at `confirmed`, holds the compartments at `requested | payment_due` (it granted in every case before). `BookingService.rejectBooking(tenantId, bookingId, { trigger, reason, hookId, bankDetails, refundPercentage, cancelledByUserId, cancelledAt, withDocument })` and `requestRejectBooking(tenantId, bookingId, { trigger, reason, bankDetails })` delegate; the `trigger` (`admin`, `customer`, `workflow`, `system`) replaces `skipWorkflow` and the refund `origin` (customer → `user`, admin → `admin`, everything else → `system`), `withDocument` replaces `skipCancellation` (the HTTP field keeps its name). The admin PUT that flips `isRejected` writes the content in the state of before and lets `cancel` (as the system, full refund) or `reinstate` move it on; a reinstatement that also confirms runs `reinstate → confirm` instead of granting with the payment open. `sanitizeBankDetails` and the customer's refund preview (`toCustomerPreview`) moved to `cancellation-refund-service.js`
- Behaviour change, deliberate (spec part 2, 4.3 and 13): cancelling a booking that is cancelled or rejected already is refused with 409 `invalid_transition` at `POST /bookings/:id/reject` and the hook release (it answered 200 with a second cancellation document before); a cancellation request for such a booking is a 409 too; an unknown booking or tenant is a 404 at both (a 500 before). The answer bodies of `POST /bookings/:id/request-reject` are unchanged. The cancellation document is issued after the state write: where it fails, the booking is cancelled without it, the mail goes out without attachment, the answer is 200 and a reprint (`POST /bookings/:id/cancellation-receipt`) issues it - before, the booking stayed uncancelled with a 500. A cancel, rejection or verification mail that fails no longer turns the cancellation or the request into a 500 (the hook, the state and the document stand). A revoke that fails is recorded before the document is issued, no longer after the workflow event. A reinstated booking that awaits payment gets its compartments held, not granted

- Booking lifecycle, step 5 of 9: the confirmation of a booking is the transition `confirm` of the lifecycle (`requested → payment_due` for a priced booking, `requested → confirmed` for a free one): the conditional state write first, then the grant and the free booking confirmation of a free booking, or the payment request (glossary "Zahlungsaufforderung") of a priced one through the new seam operation `payment.requestPayment` (a thin shell around the tenant's payment provider; a tenant without one leaves the request skipped), the workflow event `onCommit` after the write, and the organizer mail for a ticket position. `BookingService.commitBooking(tenantId, booking, { trigger })` delegates; the admin commit (`admin`), the workflow action (`workflow`) and the admin PUT name the trigger instead of `skipWorkflow`. The admin PUT that confirms writes the content as a request and lets `confirm` move it on
- Behaviour change, deliberate (spec part 2, 4.3 and 13): confirming a booking that is not a request - awaiting payment, confirmed, cancelled - is refused with 409 `invalid_transition` at `GET /bookings/:id/commit` (it answered 200 with a second payment request before); an unknown booking is a 404. A grant that fails on a free confirmation no longer restores the snapshot and answers 500: the booking is confirmed, the failure is logged and stands in the access audit log, the confirmation mail goes out. A payment request or an organizer mail that fails no longer turns the confirmation into a 500. Confirming a priced booking of a tenant without a payment service answers 200 (the undefined return answered 500 before); the organizer of a ticket booking is now told on confirmation (the block read the type off the item instead of the bookable used and never ran); the workflow event `onCommit` fires after the state write, not before

- Booking lifecycle, step 4 of 9: the effect pipeline and the first transition. `booking-lifecycle/pipeline.js` runs a transition's effects in four phases, `persist → provision → document → notify`, and owns the failure policy per adapter operation (only the store write and the hold abort, everything else is recorded in the outcome and the log); an abort restores the persist writes of the run from their snapshots. The seam is six thin adapters under `booking-lifecycle/adapters/` (store, access, documents, payment, mail, workflow); `createBookingLifecycle(adapters)` builds an instance, the default instance runs over the production adapters, tests build theirs over in-memory ones. The state write of a transition is conditional (`BookingManager.storeBookingIfStatus`, one `findOneAndUpdate` on `{ id, tenantId, status }`), so two transitions racing for one booking end with exactly one state change. `BookingService.setBookingPayed` delegates to the transition `pay` (`payment_due → confirmed`: state write, grant, receipt, workflow event, confirmation mail, organizer mail for tickets); its callers name a `trigger` (`admin`, `payment`, `workflow`) instead of `skipWorkflow`. The `mailAttach` documents of a booking are prepared by `booking-lifecycle/mail-attachments.js` (moved out of `BookingService`)
- Behaviour change, deliberate (spec part 2, 4.3): paying a booking that is not awaiting payment - a request never confirmed, a paid booking again, a free booking - is refused with 409 `invalid_transition` (`POST /bookings/:id/pay` and the payment webhook, which answered 200 with a second receipt before); an unknown booking is a 404. A receipt that fails no longer turns the payment into a 500: the booking is paid, the confirmation goes out without the receipt, a reprint issues it as a revision. The confirmation mail of a payment carries the receipt **and** the booking's `mailAttach` documents (the receipt replaced them before); the workflow event `onPay` fires after the receipt, not before the grant. The admin PUT that confirms and pays at once now writes the content at `payment_due`, confirms with the payment request and then pays (it confirmed "as if free" before)

- Booking lifecycle, step 3 of 9: booking documents are issued by one module, `documents/document-issuance.js` (`issue({ tenantId, bookingIds, type, groupBookingId? })`, `remove`): it draws the number, renders, stores the bytes and attaches the document to every booking with one atomic `$push`, so a document number exists exactly when its attachment does. The receipt of a payment and every invoice no longer ride in a whole-document write of the booking; the cancellation document is still issued before the state write of the cancellation, which carries it too, until step 6 moves it after. A second issue of a type at a booking is a revision under the same number (`revision` + 1), a reprint included: the reprint endpoints no longer hand out a fresh number. `issue` never mails. The receipt, invoice and cancellation services are renderers only; `BookingService.createReceipt`, `createAggregatedReceipt`, `createAggregatedInvoice` and `InvoiceService.issueAggregatedInvoice` are gone, the reprint endpoints and `InvoicePaymentService` call `issue`; the consistency checks of the reprints are `BookingService.reprintErrors`. Every document attachment now carries both `name` and `title`. Behaviour change, deliberate: the group reprints (`POST /group-bookings/:id/receipt`, `/invoice`) now run their consistency checks on the members, which they loaded unpopulated before, so an unpaid group is refused a receipt (`PAYED_STATUS`) and a group not paying by invoice an invoice
- Document numbers are drawn with one atomic increment (`findOneAndUpdate` with `$inc` on `<type>Count.<year>` at the tenant) instead of read-modify-write, so two documents issued at the same moment can no longer share a number. Numbers are unique, not gapless: a render or storage failure after the draw leaves a gap and is logged with number, type, bookings and error
- An aggregated document is issued exactly when the group is known, never guessed from the number of bookings: the group commit and the group payment carry `groupBookingId` to the payment provider and the receipt, the payment webhook looks the group up by its first booking

- Booking lifecycle, step 2 of 9: **schema change** - a booking stores its state as `booking.status` (`requested | payment_due | confirmed | rejected | cancelled`), the one source of truth; `isCommitted`, `isPayed` and `isRejected` stay in schema, database and HTTP answers as columns the `Booking` entity derives from the state (`isPayed` reads as "nothing left to pay": a free booking carries it in every state, a priced one once confirmed). Assigning a flag throws in a test run (`NODE_ENV=test`, set by `.mocharc.yml`) and is logged and ignored in production; the unused `BookingManager.setBookingPayedStatus` is gone. `isBookingValid()` and the status key of `GET /bookings/:id/status` read the state; a `status` in a request body is ignored, the flags stay the HTTP form
- Migration `03-09-2026-add-booking-status` reads the state off the flags of every booking (idempotent, `down` removes it) and records `cancellationRefund.cancelledFrom`, the state a cancelled booking was cancelled from (`confirmed` where paid or free, else `payment_due`), which `isPayed` is read from and `reinstate` will return to; a cancelled booking without a refund audit gets one with just that field
- New, not wired in yet: `booking-lifecycle/booking-state.js` (states, transition table `nextState`, flag derivation) and `booking-lifecycle/update-plan.js` (`planUpdate`: the admin PUT as a list of transitions, 400 `invalid_status_change` for flags without a way)
- Behaviour change, deliberate: "paid but never confirmed", a flag combination the state model does not have, reads as `confirmed` wherever it comes in (a manual booking sent with `isPayed` only, the admin PUT sending that combination, a payment webhook for a request never confirmed), so such a booking is granted, gets its receipt and the confirmation mail; the migration counts existing ones in its log. Paying or confirming a cancelled booking no longer flips a flag: the state stays cancelled. Tickets 4, 7 and 9 turn these inputs into 409/400 answers

- Locker fold, step 4 of 4: **schema change** - locker systems are rows of `accesspoints` (`type: locker`: an iFBS location, `mode: remote`, or a Pareva size located at the application's `lockerId`, `mode: authorization`), referenced by the bookables like doors; iFBS and Pareva are tenant applications of type `access`. Migration `02-09-2026-fold-lockers-into-access-points` makes the rows from `bookable.lockerDetails.units` and `booking.lockerInfo`, references them from `accessPointDetails.accessPointIds`, turns each `lockerInfo` record into an `accessInfo` compartment (confirmed → grant, unconfirmed → hold), moves the compartments made since step 3 to the rows, retypes the applications and removes `lockerDetails` and `lockerInfo`. A bookable's `amount` is lowered to its Pareva unit's where that was smaller (logged). Idempotent; `down` restores units, `lockerInfo`, the step-3 ids and the application type
- `bookable.lockerDetails` is a read field derived from the referenced locker rows, attached by the bookable routes; a value sent in is ignored. The iFBS entry of `externalProviders` follows the referenced iFBS row at write time
- The `/locker/:provider/...` routes stay one release as a facade over `AccessInfoService` and the checkout price provider: locations and sizes are the listed access points (the provider's record under `metadata`; `status` answers the same), the price is the price provider's categories, the test needs the `MANAGE_BOOKABLES` read permission only. `customer-service-info` is gone. Documented in OpenAPI as deprecated
- Both locker adapters read the tenant's application as `access` only; `AccessService` no longer synthesizes rows from `lockerDetails`. Deleted: `LockerInfoService`, the locker client and test registries, `LockerController`, `lockerApplication.js`
- Admin UI follow-up (separate card): locker systems in the access point area, `access` applications, the bookable editor on `accessPointIds`, BookingDetails on `accessInfo`; then the `/locker` facade goes
- Locker fold, step 3 of 4: the checkout runs on the access seam. `BundleCheckoutService` no longer allocates compartments (`getLockerInfo` and `adminOverrides.lockerInfo` are gone); `BookingService` holds them with `AccessService.holdForBooking` right after the booking is stored - a hold that fails rolls the booking back as before (deleted, coupon given back) - grants them with `provisionForBooking` on payment, commit and un-reject, takes them back with `revokeForBooking` on reject and delete, and moves them with `updateForBooking`; a grant that fails after the payment leaves the booking paid, the failure stands in the access audit log. The two payment entry points renew the holds with `refreshHolds` and answer a lost one as 409 `locker_unavailable` (code 3 at `POST /payments`) as before. More than one Pareva compartment of one size in one booking is now told apart by its own rental, so lowering the amount and cancelling reach every rental
- `booking.lockerInfo` is a read field derived from the `accessInfo` entries of type `locker`, in the shape it had (`id`, `lockerSystem`, `bookableId`, `isConfirmed`, `processId`, iFBS' `ifbsMetadata { boxId, nummer, price, bookingId }`); it goes out with the booking as before, a value sent in is ignored, and it is no longer stored. The seam's `Hold` and `Grant` gained an optional `metadata` (iFBS: box id and price) kept at the entry for it. Until the migration of step 4, bookings stored before this step read as having no compartments
- Until the migration of step 4 the locker systems stay configured at the bookable as `lockerDetails.units`: `AccessService` stands in for their `accesspoints` rows with synthesized ones (id `locker:<provider>:<externalId>`, capacity the unit's amount, occupancy the unrevoked compartments the concurrent bookings have at the size - bookings stored before this step count only once the migration of step 4 has made entries of them), for units whose provider the tenant has an active application for; a bookable that references a stored locker system is left to its rows. `holdForBooking` additionally drops held-only compartments beyond what an unpaid booking books after a change
- Deleted: `LockerService` (with its in-process `reservedLockers`), `BaseLocker`, `IfbsLocker`, `ParevaLocker`, the locker registry; the locker client registry and `LockerInfoService` stay for the `/locker` routes until step 4
- Locker fold, step 2 of 4: compartments are `accessInfo` entries (`accessPointType: locker`) at stored access points of type `locker`, one per compartment, told apart by `grant.authorizationId` and carrying `bookableId`, `hold`, `compartment`, `externalBookingId`. `AccessService` gains `holdForBooking` (provider hold, or a platform hold checked against `bookable.amount`: 409 `compartments_unavailable`) and `refreshHolds`; provisioning consumes the hold and always grants a compartment (iFBS' `mode: remote` too), revoking revokes each by its own grant, an update with changed time or allocation revokes and re-grants them all. The synthesis from `booking.lockerInfo` is gone; the checkout still runs through `LockerService` until step 3
- Compartments are listed and operated under an opaque id of their own (`<accessPointId>:<authorizationId>`, `<accessPointId>:hold` before the grant); `GET /access` adds `compartment` (iFBS' box number, `null` at Pareva) and reports `isProvisioned` as granted and not revoked. The access decision treats a compartment like a door that only takes a code (`not_provisioned`, `authorization_revoked`) and asks it for the evidence its locker system's rules demand. iFBS grants name the box as `compartment`; the audit log gains the action `hold`
- Locker fold, step 1 of 4: both locker providers speak the access provider seam, which gains the optional capability `hold`/`refreshHold` answering a `Hold { holdId, expiresAt, compartment }`. `IfbsAccessProvider` holds a box, confirms it as the grant, gives it back on a revoke (cancel before the usage began, end after; idempotent) and lists the locations. New `ParevaAccessProvider`: a grant is a rental (Pareva mails the code itself), a revoke cancels it, the sizes are listed; no `open`, `getStatus` or `hold`. Both find the tenant's application as `access` first and as `locker` second
- `IfbsAccessProvider` no longer declares `getStatus`, which without an open process only ever answered unknown: `GET …/status` at an iFBS locker still answers unknown, and the projection lists its capabilities as `["open"]`
- `IfbsApiClient`, `IfbsApiError` and `ParevaApiClient` moved to `access/clients/`; the Pareva client gained `listSizes`/`startRental`/`cancelRental`, and the locker stack calls it instead of raw axios. The checkout is unchanged: `LockerService` keeps running, and characterization tests pin what it leaves in `booking.lockerInfo` and at the providers
- The never-produced blocking reason `locker_not_ready` is gone from the vocabulary, the audit labels and the OpenAPI enum

- A door that only takes a code (`mode: authorization`) is operable only while its grant is there and not revoked: without one, `canOperate` and `operableAccessPointIds` in `accessEligibility` no longer include it, and close/status/open-status answer 403. Doors that also open remotely stay operable; the missing or revoked grant remains a hint in `blockingReasons`
- Open and unlatch through the API go only to doors with a remote way in (`mode: remote` or `both`); at a door that only takes a code the attempt is refused with `no_remote_access` (HTTP 200 soft failure, audited as `denied`) instead of being sent to the provider. The booking list carries the set as `remoteOperableAccessPointIds`
- The access decision is one module: `access-decision.js` answers with `decide` (role, operable access points, blocking reasons, what evidence each door demands) and `satisfy` (evidence against the rules of one door); open/unlatch, close/status, `canView`, the access point list and the bookings list all read it. No behaviour change; `accessEligibility` in `GET /access/bookings?includeEligibility=true` additionally carries `remoteOperableAccessPointIds`, `evidenceWaived` and `demandedEvidence`
- **Schema change** `booking.accessInfo[]`: the grant is stored as `grant: { authorizationId, externalPrincipalId, secret }` (secret encrypted) plus `principalRemovedAt`, `principalCleanupAttemptedAt`, `principalCleanupError`; the flat `authorizationId`, `accessId`, `saltoUserId`, `pin`, `providerResponse` and `saltoUser*` fields are gone. Migration `02-09-2026-move-access-grants` converts existing entries (idempotent, `down` restores all but `providerResponse`). `accessInfo` still goes out raw in the booking GET
- Access provider seam: `grantAuthorization` answers a `Grant`, `revokeAuthorization(accessPoint, grant)` a `Revocation`; a revoke is idempotent at every provider, so a grant already gone at the provider is recorded as revoked instead of left provisioned. Provision/revoke audit payloads carry the grant without its secret
- NUKI keypad grants send type and code as the Web API numbers them, keep the name within 32 characters, generate codes Nuki accepts (digits 1-9, not starting with 12) and read the authorization id from the listing, since Nuki creates authorizations asynchronously without answering one
- The Salto-only cleanup job is now the provider-neutral `grant-cleanup-service`, revoking again through the seam wherever a grant's external principal is still there. Env `GRANT_CLEANUP_ENABLED` / `GRANT_CLEANUP_INTERVAL_MS` replace `SALTO_KS_CLEANUP_*`, which still count where they are the only ones set

- Access provider seam typed: `open`/`unlatch` answer an `OpenOutcome`, `getStatus` a `LockStatus`, `close` nothing, and the new `getOpenProgress` (capability `getOpenProgress`, iFBS) an `OpenProgress`; the audit log stores these instead of the raw provider answer. `open`/`unlatch` throw only `AccessOpenError` at every provider now, so NUKI and iFBS failures reach the client as `openFailure` instead of HTTP 500. HTTP shapes are unchanged; a failed iFBS poll at `/open-status` reports `open`/`confirmed` as `null` (unknown) instead of `false`, and iFBS lockers no longer declare `close` (the API has no command for it)

### Added

- Mail stack, step 1 of 4: characterization tests pin every notice (glossary "Mitteilung") as it goes out today. `tests/mail-characterization.test.js` renders the 21 notices of `MailController` - the nine of the booking lifecycle through its mail adapter, the rest at the controller - over one fixture (a tenant with snippet overrides, after-texts and subject overrides; a booking with custom fields of every mail visibility; a cancellation with a refund; a group of three with a ticket; the public status view on and off) and the in-memory mail transport, and pins recipient, subject, attachment names and the HTML as snapshots under `tests/snapshots/mail/` (`UPDATE_SNAPSHOTS=1 npm test` accepts a change made on purpose). `tests/mail-recipients-characterization.test.js` pins the recipient rules as they are: the payment link takes the booking's address whatever it is given, the BCC to the tenant (the iCal counts as a document), the tenant's gate on its own notice, the supervisors without the booker, the organizers off the bookables used, the fan-out of a list of bookings, the silent exits of the transport. `tests/mail-transport-contract.test.js` is the transport contract over SMTP (nodemailer `streamTransport`), Microsoft Graph (a fake HTTP client, `tests/helpers/fake-graph-mail-client.js`) and the in-memory transport (`tests/helpers/in-memory-mail-transport.js`): the choice of account, the silent fallback, three attempts, the pool hash without the Graph secret; the target form of step 2 waits under `it.skip`. No production code changed
- `POST /api/:tenant/bookings/:id/cancellation-receipt` and `POST /api/:tenant/group-bookings/:id/cancellation-receipt` reprint the cancellation document of a cancelled booking or group as a further revision, from the stored refund audit; same right and answer as the receipt reprint, 409 `not_cancelled` without a cancellation. Documented in OpenAPI (`src/docs/routes/booking-documents.yaml`)
- Booking lifecycle, step 1 of 9: characterization tests over HTTP (`supertest`, new dev dependency) pin every state change of a booking and a group booking as it is today - admission, commit, pay, reject, cancellation request, the admin PUT and the reprint endpoints - each as a table of its effects at the seam. The known defects are pinned as today's behaviour and name the ticket that turns them. No production code changed
- Access provider contract test against NUKI, Salto KS, iFBS and an in-memory test provider, plus characterization tests that pin today's provider dialects. Access providers accept an injected API client (`{ client }`) for tests; production behaviour is unchanged

- Manual price per bookable item: `bookableItems[].manualPriceEur` (net, per unit) on the admin booking API (`PUT /api/:tenant/bookings`). Under the `ADMIN_MANUAL` checkout policy it replaces the price that categories or external providers would yield — VAT, amount and coupons apply as before — and it stays on the stored item until the admin clears it (`null`), so moving the booking never silently reprices it. A self-service checkout that carries the field has it stripped (on copies — the caller's items are left untouched), so it can never reach a stored booking. Until now the Admin UI could only express an entered price by rewriting one price category of the item, which silently missed whenever that category did not match the booking date
- QR door access: scan-code QR generation & rotation — `GET /api/:tenant/accesspoints/:id/qrcode?format=svg|png|pdf` (default `svg`) renders the printable QR code, `POST /api/:tenant/accesspoints/:id/rotate-scan-code` retires the current scan code and mints a new one. Requires the new global env var `STORE_FRONT_URL` (public store-front base URL, used to build `https://<STORE_FRONT_URL>/mobile-key/<tenant>/<scanCode>`; not the Vue admin app that `FRONTEND_URL` points at)
- QR door access: location prefill from the provider — `GET /api/:tenant/accesspoints/:id/location-prefill` suggests where the lock stands via the optional provider capability `getLocation` (NUKI returns coordinates without address; Salto KS and providers without the capability return `null`). `getLocation` support is exposed in `providerCapabilities` of `GET /api/:tenant/access-apps/providers`. The endpoint writes nothing; adopt the suggestion via `location` in the regular `PUT /api/:tenant/accesspoints`

- Salto KS remote open per the spec: IQ activation wizard under `GET/POST/DELETE /api/:tenant/access-apps/salto-ks/iqs…` (`MANAGE_TENANTS`; first secret stored encrypted before the PIN mail, PIN entered once by the admin, activation via `PUT …/pin` with `delta:"0000"`), the backend computes the Salto-OTP itself on every open (max one per attempt; `otp_blocked` starts a 25-min local backoff, 3× `otp_invalid` degrades the activation), and `supportedModes` follow the capability rule (keypad lock types keep `authorization`, `remote` needs an activated IQ — or none for IQs without `otp_enabled` — and no `restore_required`). `POST …/salto-ks/test` additionally reports `remote_access` and the per-IQ activation state; the remote locking right is declared not verifiable

- Media library core: new `media` collection (schema/model/entity/manager) as the single source of truth for platform-managed files, plus a shared media reference sub-document (`{ source: "media" | "external" }`) for later use at bookables, events and the instance
- Storage abstraction with exactly seven operations (`put`, `getStream`, `getBuffer`, `stat`, `delete`, `deleteMany`, `deletePrefix`) and a provider-neutral `StorageError`; Nextcloud (WebDAV) and S3 (AWS SDK v3, `S3_ENDPOINT` + `S3_FORCE_PATH_STYLE` make MinIO pure configuration) implement it. Keys follow the media identity (`{tenantId}/media/{mediaId}/original.{ext}`, instance media under `_instance/`); reading follows the provider stored on the medium, `STORAGE_PROVIDER` only steers new uploads
- `/api/v2/:tenant/media` endpoints: upload (one file per request), paginated listing with `kind`/`tag`/`q`/`visibility` filters, metadata read and PATCH (never the file), original download and delete (database document first, bytes best-effort). `public` media are readable anonymously, `intern` media require an active membership in the owning tenant. Documented in OpenAPI from the start
- Boot check for the storage configuration: an explicitly set `STORAGE_PROVIDER` with missing variables aborts the boot; the implicit Nextcloud default only warns
- Media upload hardening: the type is decided from the content (magic bytes via `file-type`, plus a sharp decode for images), never from the file name; the allowlist is JPEG, PNG, WebP, GIF, SVG and PDF. Per-kind limits come from `MEDIA_MAX_IMAGE_SIZE_MB` (15) and `MEDIA_MAX_DOCUMENT_SIZE_MB` (50)
- Global `express-fileupload` limit for every upload route (`MEDIA_UPLOAD_BACKSTOP_SIZE_MB`, by default 5 MB above the largest media limit) — the upload middleware ran without any option before
- Image variants `thumb` (160×160 crop), `sm`/`md`/`lg` (480/800/1600 wide, never enlarged), all WebP, generated synchronously on upload: a failing variant fails the whole upload and already written bytes are removed again. Presets that would not shrink the original produce no variant, SVG originals are rasterised for all presets, animated GIFs become stills, documents get none
- `GET /media/:id/file?size=<preset>` serves variants and degrades to the next larger variant and finally to the original, so a preset choice never 404s; an unknown preset name is a 400. SVG originals are served as a download
- Caching matrix for media delivery: public originals immutable for a year, public variants cacheable for a day with a strong ETag from the variant checksum, internal media `private, no-cache` with an ETag, booking documents `private, no-store` — including `304` handling, in a shared helper (`commons/utilities/cache-headers.js`)

- Role group `manageMedia` (the usual seven booleans, ownership is the uploader) and the admin interface `media`; media create/update/delete run through the `PermissionService` with own/any semantics. Listing and metadata reading are the picker right (`readAny` shows the whole library, `readOwn` only one's own uploads); the binary route keeps following the visibility of the medium
- Migration `26-08-2026-add-manage-media-role-group` mirrors `manageBookables` into `manageMedia` for every role and adds the `media` admin interface wherever a bookable interface is managed, so no existing admin loses a workflow
- Receipts, invoices and cancellations are stored as booking document media (non-empty `bookingIds`) instead of raw Nextcloud files. An aggregated document is one medium with one byte copy, referencing every booking of its group. Reading follows the receipt rule — `manageBookings.readAny` or the owner of any referenced booking, so customers reach their own invoices without a role — and the existing booking download routes keep working as a facade, falling back to the legacy Nextcloud tree for documents written before this change. Booking documents never appear in the library listing or picker, and cannot be deleted through the API

- `GET /api/v2/:tenant/media/:id/usage` answers the usage proof of a medium — every bookable, event, booking and the instance that references it, searched on demand over the reference sites; there is no `usedBy` field and no back reference at the medium. Read like the metadata
- Deleting a medium that is still in use answers `409` with exactly the body of `/usage`, so the admin UI shows one list no matter which call produced it. Without usage, deletion stays final: database document first, bytes best-effort — the whole storage folder of the medium is removed (`deletePrefix`), so no empty folder stays behind on WebDAV; a failed byte removal leaves an accepted orphan for the later media CLI to clean up
- Removing a booking now cascades to its documents (system receipts included): each document loses that booking's reference before the booking itself goes, and a medium is deleted with its last reference, on the same database-first, bytes-best-effort path
- One imported legacy file is one medium per tenant: partial unique index on `{tenantId, legacyPath}` (built once pre-model-change imports are purged); deliberately no checksum-based dedup — deduplication follows file identity (legacy path, booking group), never content

- Bookables carry an ordered image list (`images`) of media references instead of a single image; position 0 is the cover image, so reordering the list changes the cover and an empty list means there is none. `imgUrl` stays in exports as a derived read field with the address of the cover image, so storefront v4 and the HTML endpoint are unchanged — the HTML endpoint still shows only the cover image
- Every image site of an event is a media reference now — teaser image, contact person image, the image list (`images`) and the photo of each speaker (`eventOrganizer.speakers[].image`). All of them keep going out under their old names as the address they resolve to, so the public structure and the HTML markup are unchanged; the image list stays a plain list, the title image of an event is and stays the teaser image. With that the usage proof covers the whole event, and deleting a medium that only hangs on a speaker or in the image list is blocked instead of silent
- Attachments of bookables, events and bookings carry a media reference under `reference` next to their context fields (`title`, `caption`, `show`, `required`, `mailAttach`); the checkout copies it into the booking. Exported references are enriched with the delivery URL
- Saving a bookable or event validates every medium it references: it has to belong to the tenant, the saver needs the picker right, and a publicly visible entity may only reference `public` media
- `mailAttach` attachments that are media references are read through the media service instead of an HTTP self-call to the platform's own public URL — which is why `mailAttach` now works for `intern` media too. External references are still fetched over HTTP
- Media images in mails follow the visibility of the medium: a public image is embedded at `?size=sm`, an intern one is only linked, since a mail client fetches images anonymously

- `/api/v2/instance/media` opens the media library for instance-wide content: the same routes as the tenant library, addressed with `instance` where a tenant id would stand, for media without a tenant (keys under `_instance/`). Managing it belongs to the instance owner alone, and `intern` means any signed-in user of the instance — there is no tenant whose membership could narrow it. Instance and tenant media stay strictly separated in both directions: neither appears in the other's listing, and neither can be referenced from the other's entities
- Instance branding and legal documents carry media references (`branding.logo`, `branding.favicon`, and `reference` inside `dataProtection`/`legalNotice`/`termsAndConditions`); `branding.logoUrl`/`faviconUrl` and the `url` of each legal document stay as derived read fields, so nothing downstream has to learn about media. Both are absolute addresses, as branding always was: the store front fetches the logo server side and puts a document URL into an `href`, and has no origin of the platform to resolve a relative one against. Saving them validates that the medium is an instance medium the instance owner may pick, and branding takes `public` media only
- ICO joins the upload allowlist for favicons. It passes on its magic bytes alone and gets no variants — sharp cannot read it
- `media-cli relocate --to <nextcloud|s3> [--tenant <id>]` moves the media stock between storage providers: per medium copy all files under unchanged keys, verify size (and checksum where stored) at the target, then flip `storage.provider` — a medium moves as a whole or not at all, and the bytes at the old provider stay in place for the operator to remove

- Media CLI (`node src/cli/media-cli.js`) with `import`, `regenerate`, `verify`, `cleanup`, `purge-imported`, `purge-legacy` and `relocate`, each with `--dry-run` and a report of what was processed, skipped, left unplaced or failed. Deliberately not a boot migration: an update never waits on a file move. Every command is idempotent — a second run changes nothing
- `import` turns the whole legacy file stock into media: `public/` and `protected/` of every tenant and the tenant-less trees (which become instance media), folder names become tags, the tree decides the visibility, and the place a file had is kept as its legacy path. Bytes are copied to the currently configured storage provider, so the import is the storage move as well; the source is left where it is and `uploadedBy` stays empty. Variants follow with `regenerate`
- `import` then converts every stored address into a media reference — bookable cover images and attachments, every image site of an event and its attachments, instance branding and legal documents, and the attachment copies on bookings. Resolution is host-independent: stored URLs carry the host of the environment they were uploaded in, so only the path decides. Addresses that are not ours stay external references, and tenant and instance scopes never resolve into each other
- Legacy booking documents (`receipts/`, `invoices/`, `cancellations/`) are placed by matching their file name against the `title` and `name` of the booking attachments; one legacy file becomes one medium referencing every booking it names. A file no attachment names is reported, never guessed
- `purge-imported` takes an import back so it can run from scratch: it removes every medium carrying a legacy path (`--tenant` narrows it to one tenant) and leaves the legacy tree, which is what the next `import` reads, untouched. Entities that still reference one of the media stop the whole run before anything is deleted, since the import may already have rewritten stored addresses. Bytes go best-effort, so a key the storage keeps is reported as an orphan — `cleanup` can no longer reach the key space of a deleted medium. The report names the duplicate legacy-path groups it found and what each scope is left with
- `GET /api/files/get` and `GET /api/:tenant/files/get` stay for good as the resolver of stored legacy addresses: they look a medium up by its legacy path and deliver it with the media caching matrix. Until the import has run they still serve the legacy tree directly — but with the media permission checks in front of it, so a protected file now needs an active membership in the owning tenant instead of any session at all. Once the import has run, the library is the whole truth and an unknown address is a `404`
- An installation whose media import has not run boots with a warning, never an error

- Tenants carry their own legal documents (`legalDocuments`): a list of `{ type, title, reference }` with the types `dataProtection`, `legalNotice`, `termsAndConditions`, `rightOfWithdrawal` and `other`. A known type may appear only once and carries no title; `other` needs one and no two may share it. Unlike the instance documents this is a typed subdocument list — the tenant has no legacy stock to keep readable. Migration `28-08-2026-add-tenant-legal-documents` starts the field empty everywhere
- Tenant legal documents are a media reference site: the usage proof and the deletion block cover them, so a medium a tenant document references answers `409` with the new usage type `tenant` instead of being deleted. The matching save-time check is in place in the media reference guard — the medium has to belong to the tenant, the saver has to be allowed to pick it, and it has to be `public`, since a legal document is meant to be published — and takes effect once the tenant API accepts the field
- `legalDocuments` travels with the existing tenant API, without an endpoint of its own: `PUT /api/tenants` takes the field and refuses a broken list or a medium that may not be referenced with its own code instead of a blanket `500`, and the tenant reads back with every reference carrying the address it resolves to. The permission is the one that was already there, tenant or instance ownership, and the public tenant export is unchanged

### Fixed

- Defects found in passing by the architecture review of 2026-09-01: booking references are now really checked for uniqueness before a new booking takes one (the check read `.id` off a promise and never fired), `GET /api/payments/response` no longer treats `aggregated=false` as aggregated, checkout checks name their own reason code (`checkout.duration_too_short`, `checkout.duration_too_long`, `checkout.bookable_not_bookable`, also for the iFBS minimum duration) instead of the resolver guessing it from German prose, two stray `console.log` calls are gone and the receipt service and the PDF browser log under their own names
- A mandatory checkout addon that was already in the cart with a mismatched amount is priced once instead of twice — the checkout corrected the amount but also appended the same item again, so `POST /checkout` charged the addon double while `validate-group` priced it correctly
- The public bookable routes (`/bookables/public`, `/bookables/public/:id`) resolve media references again — they serve the raw entity, whose stored references carry no `url` since the media library, so the storefront checkout lost its cover image (`imgUrl` empty, `images[].url` null) while the JSON embed interface kept working through `exportPublic`
- Nextcloud storage uses the `/remote.php/dav/files/{user}` endpoint instead of the legacy `/remote.php/webdav`. The legacy endpoint answers PROPFIND on a missing path with a malformed 207 multistatus instead of a 404, which crashed the webdav client during recursive directory creation and made every media upload fail with `storage_put_failed`
- The HTML and JSON embed interfaces resolve media addresses to absolute URLs (`BACKEND_URL`) again — cover images (`imgUrl`), image lists, event teaser/speaker images and attachment links were relative delivery routes since the media library, which foreign websites embedding the interfaces cannot load. The storefront and admin API keep their relative URLs
- Attachments with a media reference render a working link in the HTML detail views — the raw stored entry has no `url` anymore
- Storage failures no longer write credentials into the log. A raw webdav or axios error carries its whole request — `Authorization` header and session cookies included — and bunyan without an error serializer wrote all of it out; the media, storage and legacy-file loggers now use the standard serializer, which keeps the message and the stack and nothing else
- A legacy tree that does not exist is logged at debug instead of error. The media import asks after every tree an installation could have, and an installation without cancellations does not have a `cancellations/` folder — those misses buried the report the command exists to produce

### Changed

- Checkout policy: one value (`SELF_SERVICE` | `ADMIN_MANUAL`) crosses the checkout interface instead of the flag combination `manualBooking` + `bookWithoutDiscount` + `capacityChecksOnly` + `excludeBookingIds`. What a policy means is decided in one module (`checkout-policy.js`): under `ADMIN_MANUAL` no checks run, discounts are always suppressed, mandatory addons are not auto-added, admin-entered prices are authoritative and the invoice permission is not checked. The `Manual*` checkout service classes are gone; the base services interpret the policy, accept an admin-overrides object (`ADMIN_MANUAL` only) and take an optional bookable snapshot in `init`. Behavioural fixes riding along: external booking providers now receive the checkout id (`userID`) on manual bookings — the manual constructors silently dropped it — and all items of one checkout share one external-provider cache
- Terminology: `CONTEXT.md` defines „Checkout-Policy", „Selbstbuchung" and „Manuelle Buchung"
- Access open/unlatch: the `otp` request parameter is gone for good — providers that need an OTP compute it themselves. Provider failures after passed checks now answer soft (`success: false`, `data.openFailure: "temporary" | "configuration"`) instead of HTTP 500; details stay in the access audit
- Docs: Salto KS remote open specified after the successful door proof — integration spec `docs/specs/salto-ks-remote-open.md`, ADR 0002 (IQ activation via API only, never via the app), ADR 0001 status updated to proven, glossary entries corrected
- Docs: Salto KS research corrected to the proven state — API contract §9 resolution (app activation rotates the secret, real `delta` causes the PIN ambiguity; the API path works), positive-run addendum in the door-proof doc, remote-open status in the access-points tracking board
- Salto KS access app: the free-text `apiBaseUrl` is replaced by `environment` (`accept` | `production`, default `accept`); the backend derives Connect API and identity server from it (accept: `clp-accept-user.my-clay.com` + `identity-acc.eu.my-clay.com`, production: `connect.my-clay.com` + `identity.eu.my-clay.com`, overridable via `SALTO_{ACCEPT,PRODUCTION}_{API_BASE_URL,IDENTITY_URL}`; the global `SALTO_IDENTITY_URL` is gone). A stored legacy `apiBaseUrl` is tolerated and only read to tell the environment. `POST /api/:tenant/access-apps/salto-ks/test` accepts `environment` and now reports the identity server's `error`/`error_description` (e.g. `invalid_client: …`) or the Connect API `Message` instead of the bare HTTP status
- The media CLI resolves its `.env` from the repository rather than the working directory, so it runs from anywhere; a failing command prints its error instead of the whole help text
- Who may read the file of a medium now lives in one media access service instead of the media controller, so the media route and the legacy resolver answer the question identically
- `applyCacheHeaders` also owns `Last-Modified` and its `If-Modified-Since` comparison, for the legacy resolver, which serves bytes it has no checksum for

### Removed

- The tenant-less `GET /api/files/list` and `POST /api/files` are gone, replaced by `/api/v2/instance/media`; the upload route ran without any authentication middleware
- The tenant file listing `GET /api/:tenant/files/list` and the upload `POST /api/:tenant/files` are gone with the admin UI switching to the media library. `GET /api/:tenant/files/get` stays as the resolver of legacy addresses

### Notes

- Booking documents are the system receipts the platform writes itself; the API refuses to delete them (`booking_document_not_deletable`), they only cascade with their booking
- Legacy plain URLs (`imgUrl` of a bookable, the image fields of an event, the `url` of an attachment) stay readable and are read as external references until the media import converts them; nothing is rewritten on save
- `purge-legacy` removes only files a medium answers for; whatever the import could not take — an unplaced booking document above all — stays in the tree and is reported. `cleanup` reaches the key space of known media only, because the storage contract has no `list`: bytes of an already deleted medium remain the operator's to remove
- New dependencies `sharp` and `file-type`; sharp ships prebuilt binaries, tune it with `MEDIA_IMAGE_MAX_PIXELS` and `MEDIA_SHARP_CONCURRENCY`

## [4.2.6] — 2026-08-25

### Added

- Custom field definitions support `usageOptions.showInMail` (checkout fields only, enforced on write): flagged fields render as `Label: Wert` lines in the booking-details block of all booking mails; empty values show as "nicht angegeben"
- Mail-visible custom field values render type-aware via `CustomFieldService.formatValueForDisplay`: booleans as "Ja"/"Nein", selects as the option caption (raw value if the option was deleted), numbers as strings
- Migration `25-08-2026-customfield-show-in-mail` backfills `usageOptions.showInMail: false` on existing custom field definitions (instance, tenant, bookable) so all fields stay opt-in

### Fixed

- Admin booking update no longer applies the assignee's bookable booking discounts when recomputing prices (same as manual create; Admin-entered list prices from `priceCategories` are kept)

## [4.2.5] — 2026-08-10

### Fixed

- Admin manual booking create never hard-fails on checkout rules (including capacity/overlap), matching admin update; use validate endpoints for informational availability

## [4.2.4] — 2026-08-10

### Fixed

- Manual/admin booking create no longer applies the creating user's bookable booking discounts (list price is kept; Admin UI does not send `bookWithoutDiscount`)
- Admin booking update never hard-fails on checkout rules (including capacity/overlap); prices are still recomputed from edited `_bookableUsed.priceCategories`. Use validate endpoints with `excludeBookingIds` for informational availability while editing

## [4.2.3] — 2026-07-31

### Added

- Tenant setting `mailBookingPeriodFormat` (`default`, `fromTo`, `timeFirst`, `long`, `compact`) to control how booking periods are rendered in email booking details

## [4.2.2] — 2026-07-29

### Added

- Booking mails support an optional editable closing snippet (`mailSnippets["{type}__after"]`) rendered after buttons, QR code, and the system footer
- Tenant setting `mailShowSupportFooter` (default `true`) to hide the automatic support-contact footer in booking mails

### Fixed

- **DEV-845:** Group cancellation refund preview lists bookings chronologically by `timeBegin` (with dates in the payload); `createGroupBooking` also sorts attempts by start time before creating
- Mail snippets inherit the tenant mail theme font; hardcoded `font-family` values are stripped at render time so booking details match header/footer typography
- Normalize double-quoted font names in mail HTML (`"Segoe UI"` → `'Segoe UI'`) so inline theme styles are not truncated

## [4.2.1] — 2026-07-22

### Added

- `POST /api/v2/:tenant/checkout/validate-group` — batch-validate series / group booking attempts in one request (avoids storefront 429s from parallel single validates)
- Full-stack operator guide ([getting-started.md](getting-started.md)): wire Admin UI and Storefront to the API, local npm setup, and Docker Compose example (`docker-compose.full-stack.example.yml`)
- Public JSON bookable responses (`/json/:tenant/bookables*`, event tickets) include the tenant `cancellationRefundTiers`

### Fixed

- Docker image base switched from Node 25 to Node 22 LTS so `npm ci` succeeds during image build

## [4.2.0] — 2026-07-17

### Added

- Configurable cancellation refund tiers: refund share can depend on how many days before the booking the cancellation happens; admins can preview and override amounts
- Customers see expected refund amounts before and after self-cancellation (preview and confirmation emails)
- Percentage booking discounts per user or role on bookables (replaces free-booking-only lists); coupons still apply afterwards
- Optional contact hint in booking emails when self-cancellation is disabled, so customers know how to get in touch
- Supervisor notifications on new bookings (single and series), with configurable recipients per membership
- Catalog indicates whether the current user may create series bookings
- Bank details can be included on group booking cancellations (as with single bookings)
- Renaming a user can optionally skip updating names on assigned self-bookings

### Fixed

- Fixed-amount coupons are applied correctly to the total (gross) price and only once per checkout, also for multi-item bookings
- Invoices and receipts present coupon and discount lines more clearly (regular prices, Rabatt labels, discounts after VAT)
- Cancellation PDFs: clearer refund labels, improved table layout, and optional cancellation number prefix only when configured
- No cancel button in booking emails when self-cancellation is turned off
- Deleting custom field definitions also removes their values from affected bookables
- SSO users can again access related bookings, protected files, and private catalogs without token errors
- Bookings created manually by an admin appear in the assigned user’s personal booking list
- Restoring a previously cancelled booking clears stored refund audit data

## [4.1.4] — 2026-07-03

### Added

- Preview for receipt, invoice, and cancellation PDF templates before saving
- Configurable layout for booking tables in PDFs (summary, compact, detailed)
- Option to show or hide booking number, period, and payment details in PDF tables
- Manual collective invoice for all bookings in a group booking, with optional email delivery
- Page numbers and repeating headers/footers on multi-page PDFs

### Changed

- Updated default templates for receipts, invoices, and cancellations

## [4.1.3] — 2026-07-02

### Fixed

- Invitation partial unique index definitions now use MongoDB-compatible predicates in partial filters to avoid startup failures on environments that reject `$ne` in partial index expressions

## [4.1.2] — 2026-07-02

### Added

- User ID normalization utility for case-insensitive email/user matching across invitation and membership flows
- Migration `02-07-2026-normalize-membership-invitation-user-ids` to normalize invitation/membership user IDs and deduplicate conflicting membership and single-use invitation records

### Changed

- Preparation lead time (`preparationLeadTimeMinutes` / `serviceHours`) now also applies to time-period- and block-period-related bookables, not only schedule-related ones

### Fixed

- Invitation acceptance, rejection, and verification now compare intended user IDs case-insensitively
- Tenant user onboarding and invitation resend endpoints now normalize user IDs before membership/invitation lookups
- Prevent creating duplicate active single-use invitations for the same tenant and intended user

## [4.1.1] — 2026-07-01

### Fixed

- Invoice number generation now uses `invoiceNumberPrefix` instead of `receiptNumberPrefix`

## [4.1.0] — 2026-06-30

### Added

- Preparation lead time for schedule-related bookables: `preparationLeadTimeMinutes` and weekday-based `serviceHours` on the bookable schema
- Lead-time calculator and checkout availability checks ensuring a contiguous preparation block within service hours before booking start
- Booking buffer times (`bufferTimeBeforeMinutes`, `bufferTimeAfterMinutes`) for capacity checks to prevent back-to-back bookings
- Bookable entity documentation for lead-time and buffer configuration

### Changed

- Capacity interval calculator, calendar service, and booking manager apply buffer-expanded blocked intervals around existing bookings
- Window availability and parent/child capacity rules account for booking buffers

## [4.0.1] — 2026-06-26

### Added

- Entity and API documentation updates

## [4.0.0] — 2026-06-26

- Major release line on branches `develop` (integration) and `version/4.x` (stable)
- Public booking UI moved to [smart-city-booking-store-front](https://github.com/ECCdigital/smart-city-booking-store-front)
- [smart-city-booking-vue-app](https://github.com/ECCdigital/smart-city-booking-vue-app) remains the primary **Admin UI**

## Earlier releases

### v3.7.x (LTS)

- Maintenance on branch `version/3.x`
- Patch and security releases tagged `v3.x.x`

### v3.4.0 (BREAKING)

- JWT-based authentication replaces session/cookie login
- **Membership** entity replaces `tenant.users` and `tenant.ownerUserIds`

**Migration guide:** [migrations/v3.4-jwt-membership.md](migrations/v3.4-jwt-membership.md)

See git tags (`v3.x.x`, `v2.x.x`, `v4.0.0-rc.*`) for historical releases.

[4.2.6]: https://github.com/ECCdigital/smart-city-booking-backend/compare/v4.2.5...v4.2.6
[4.2.5]: https://github.com/ECCdigital/smart-city-booking-backend/compare/v4.2.4...v4.2.5
[4.2.4]: https://github.com/ECCdigital/smart-city-booking-backend/compare/v4.2.3...v4.2.4
[4.2.3]: https://github.com/ECCdigital/smart-city-booking-backend/compare/v4.2.2...v4.2.3
[4.2.2]: https://github.com/ECCdigital/smart-city-booking-backend/compare/v4.2.1...v4.2.2
[4.2.1]: https://github.com/ECCdigital/smart-city-booking-backend/compare/v4.2.0...v4.2.1
[4.2.0]: https://github.com/ECCdigital/smart-city-booking-backend/compare/v4.1.4...v4.2.0
[4.1.4]: https://github.com/ECCdigital/smart-city-booking-backend/compare/v4.1.3...v4.1.4
[4.1.3]: https://github.com/ECCdigital/smart-city-booking-backend/compare/v4.1.2...v4.1.3
[4.1.2]: https://github.com/ECCdigital/smart-city-booking-backend/compare/v4.1.1...v4.1.2
[4.1.1]: https://github.com/ECCdigital/smart-city-booking-backend/compare/v4.1.0...v4.1.1
[4.1.0]: https://github.com/ECCdigital/smart-city-booking-backend/compare/v4.0.1...v4.1.0
[4.0.1]: https://github.com/ECCdigital/smart-city-booking-backend/compare/v4.0.0...v4.0.1
[4.0.0]: https://github.com/ECCdigital/smart-city-booking-backend/releases/tag/v4.0.0
